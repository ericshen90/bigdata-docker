docker build -t centos-java centos-java
docker build -t docker-zk docker-zk
docker build -t docker-hadoop docker-hadoop
docker build -t docker-mysql docker-mysql
docker build -t docker-hive docker-hive
docker build -t docker-spark docker-spark
docker build -t docker-hbase docker-hbase
